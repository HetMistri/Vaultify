# Database Configuration for Docker Compose
# PostgreSQL service configuration
POSTGRES_DB=vaultify_db
POSTGRES_USER=vaultify_user
POSTGRES_PASSWORD=secret123

# Application database connection (passed to Java app container)
# These map to Config.java keys: db.host, db.port, db.name, db.user, db.password
DB_HOST=db
DB_PORT=5432
DB_NAME=vaultify_db
DB_USER=vaultify_user
DB_PASSWORD=secret123

# Instructions:
# 1. Copy this file to .env: cp .env.example .env
# 2. Update POSTGRES_PASSWORD and DB_PASSWORD with secure values
# 3. Both should match for Docker Compose to work correctly

